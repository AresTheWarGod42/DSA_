on:
  push:
    branch:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compile check
        run: |
          find . -name "*.cpp" -exec g++ -std=c++17 -c {} \;
